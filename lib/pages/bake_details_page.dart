import 'package:flutter/material.dart';

class BakeDetailsPage extends StatelessWidget {
  const BakeDetailsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Details'),
    );
  }
}