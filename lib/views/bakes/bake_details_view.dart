import 'package:flutter/material.dart';

class BakeDetailsView extends StatelessWidget {
  const BakeDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Details'),
    );
  }
}