import 'package:flutter/material.dart';

class BakeListView extends StatelessWidget {
  const BakeListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('BackList'),
    );
  }
}